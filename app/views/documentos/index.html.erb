<p id="notice"><%= notice %></p>

<h1>Documentos</h1>

<div class='list-group'>
  <% @documentos.each do |documento| %>
    <%= link_to 'Show', documento_path(documento), :class=>'list-group-item list-group-item-action' %>
  <% end %>
</div>
<br>

<%= link_to 'New Documento', new_documento_path, :class=>'btn btn-primary' %>
