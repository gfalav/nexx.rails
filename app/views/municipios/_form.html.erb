<%= form_with(model: municipio, local: true) do |form| %>
  <% if municipio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(municipio.errors.count, "error") %> prohibited this municipio from being saved:</h2>

      <ul>
      <% municipio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= 'Departamento: ' + municipio.departamento.nombre %>
    <%= form.hidden_field :departamento_id, :class=>'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, :class=>'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar", :class=>'btn btn-danger' %>
  </div>
<% end %>
